test-runner:
  image: alpine:latest
  script:
    - echo "=== Новый раннер с новым токеном ==="
    - echo "Токен был пересоздан успешно!"
    - echo "SUCCESS: Runner is working with new token"
    - uname -a
    - echo "Все тесты - пройдены!"
  tags:
    - docker
